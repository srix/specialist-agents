# Local Development

**Last updated:** {{TODAY}}

Setup steps and environment notes for working on {{PROJECT_NAME}} locally. Architectural decisions live in [`architecture.md`](./architecture.md).

## Prerequisites

- <toolchain version>
- <SDK / runtime>
- <credentials / env files>

## First-time setup

```bash
# clone, install deps, etc.
```

## Common tasks

### Running locally

```bash
# how to start the dev loop
```

### Running tests

```bash
{{VERIFY_COMMAND}}
```

### Building a release artifact

```bash
# build command(s) for a shippable artifact
```

## Secrets / credentials

<!-- Where local-only files live (keystore.properties, .env, etc.), what's in version control vs. ignored. -->

## Known gotchas

<!-- Things that bit you once and will bite again. -->
